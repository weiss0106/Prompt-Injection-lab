version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    command: ["/app/frontend/wait-for-backend.sh"]
    ports:
      - "5174:5174"
    depends_on:
      - backend
      - ollama
    volumes:
      - ./frontend/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./frontend/wait-for-backend.sh:/app/frontend/wait-for-backend.sh:ro
    restart: always

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    command: ["python", "-m", "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8001"]
    working_dir: /app/backend
    ports:
      - "8001:8001"
    environment:
      - OLLAMA_HOST=ollama
    depends_on:
      - ollama

  ollama:
    image: ollama/ollama:latest
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
      - ./ollama-entrypoint.sh:/ollama-entrypoint.sh:ro
    entrypoint: ["/bin/sh", "/ollama-entrypoint.sh"]

volumes:
  ollama_data:
